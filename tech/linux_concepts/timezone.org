*Method One*

Remove the currently set timezone

> rm /etc/localtime

Set the new timezone from the zones in the directory

> ln -s /usr/share/zoneinfo /etc/localtime

*Method Two (on some distros)*

Change the the /etc/timezone

with text editor

> vim /etc/timezone

or

> export TZ=Europe/London
