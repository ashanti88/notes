Every partition sets aside 5% as default of reserve space. So, when it's
100%, it's actually at 95%. To increase this momentarily...

df -h

/dev/whatever

then

tune2fs -m2 /dev/whatever

'2' denotes the percentage you want it to set it too (so this would be
98% full now)

Once you clear some space, you can always set it back to 5% using the
same command,
